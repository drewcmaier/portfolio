# Drew Maier's portfolio site

Powered by [`svelte`](https://svelte.dev/).

## Running

Once you've installed dependencies with `npm install` (or `pnpm install` or `yarn`), start a development server:

```bash
npm run dev
```
